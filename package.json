{
  "name": "crowdmap",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node server",
    "start:eb": "cd web-client && npm install --production && npm run build && cd .. && npm start"
  },
  "dependencies": {
    "osc": "^2.3.1",
    "uuid": "^3.3.3",
    "ws": "^7.4.6"
  }
}
